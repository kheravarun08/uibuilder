// Sidebar.jsx — orchestrator
// Wires SidebarUI into the app.
// Add any future sidebar-level logic here (e.g. search, category filters).

import SidebarUI from "./SidebarUI";

export default function Sidebar() {
  return <SidebarUI />;
}